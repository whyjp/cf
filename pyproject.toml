[project]
name = "cf-workspace"
version = "0.0.0"
description = "cf monorepo — crawl/{camfit,txcp} + backend + pipeline (uv workspace root, not published)"
requires-python = ">=3.11"

[tool.uv.workspace]
# Members get added as each sprint moves a package into place.
members = ["crawl/txcp", "crawl/camfit"]

# [tool.uv.sources]
# cf-backend = { workspace = true }   # uncomment when backend/ exists (Sprint 5)
